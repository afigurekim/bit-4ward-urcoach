spring:
    mvc:
        view:
            prefix: /
            suffix: .html
    datasource:
        driver-class-name: org.mariadb.jdbc.Driver
        url: jdbc:mariadb://localhost:3306/mariadb
        username: mariadb
        password: mariadb
server:
    port: 9000